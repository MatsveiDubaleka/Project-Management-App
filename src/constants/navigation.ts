export enum Navigation {
  BOARDS = '/boards',
  BOARDSSET = '/boardsSet',
  COLUMNS = '/columns',
  SIGN_IN = '/signin',
  SIGN_UP = '/signup',
  TASKS = '/tasks',
  HOME = '/',
}

export default Navigation;
